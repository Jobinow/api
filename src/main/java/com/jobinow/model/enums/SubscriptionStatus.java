package com.jobinow.model.enums;

/**
 * Enum representing the status of a subscription.
 */
public enum SubscriptionStatus {
    /**
     * Indicates that the subscription is active and ongoing.
     */
    ACTIVE,

    /**
     * Indicates that the subscription has been terminated or completed.
     */
    TERMINATED
}
